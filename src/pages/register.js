import React, { Component } from "react";
import { Layout } from "../components";

export default class Register extends Component {
  render() {
    return <Layout>Register new user:</Layout>;
  }
}
